'use strict'

import initialState from '../initialState'

const reducer = (prevState = initialState, action) => {

  let newState = Object.assign({}, defaulState)

  switch (action.type) {
  }

  return newState
}